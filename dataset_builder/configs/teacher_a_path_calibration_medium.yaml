dataset_root: /home/robot-device/yangyujie/BEV_LIMO/LIMO_DATASET
output_dir: /home/robot-device/yangyujie/Try2/teacher_a_path_calibration_medium
missions:
  - 2024-11-02-21-12-51
  - 2024-11-03-07-52-45

map_size: 4.0
map_resolution: 0.04
dense_sampling_interval_m: 0.02
timestamp_tolerance_s: 0.05
tolerant_forbidden_ratio: 0.05

# Sampling is by unique image_id, never by raw path row.
geometric_target_images_per_mission: 200
geometric_paths_per_image: 2
teleop_target_images_per_mission: 200

# Values are already grid-realizable except the conservative 0.61 request,
# which remains solely as the 0.64 m effective-radius upper-bound control.
radius_scan_m: [0.0, 0.20, 0.24, 0.28, 0.32, 0.36, 0.40, 0.61]

device: cpu
seed: 42
mppi_config: dataset_builder/configs/build.yaml

