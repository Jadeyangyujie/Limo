dataset_root: /home/robot-device/yangyujie/BEV_LIMO/LIMO_DATASET
missions:
  - 2024-11-02-21-12-51
  - 2024-11-03-07-52-45

# Diagnostic-only output. No formal dataset or Zarr is written.
output_dir: /tmp/limo_teacher_a_stage1

map_size: 4.0
map_resolution: 0.04
root_xy: [0.0, 0.0]
inflation_radii_m: [0.0, 0.26, 0.61]

# Only these evenly spaced candidates are evaluated for representative-frame
# selection; selected frames are then rendered in detail.
candidate_frames_per_mission: 24
selected_frames_per_mission: 6

device: cpu
seed: 42

# Reuse the complete traversability settings from this existing config.
mppi_config: dataset_builder/configs/build.yaml
